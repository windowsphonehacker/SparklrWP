<phone:PhoneApplicationPage 
    x:Class="SparklrWP.Pages.Profile"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:phone="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone"
    xmlns:shell="clr-namespace:Microsoft.Phone.Shell;assembly=Microsoft.Phone"
    xmlns:controls="clr-namespace:Microsoft.Phone.Controls;assembly=Microsoft.Phone.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    mc:Ignorable="d" d:DesignWidth="480" d:DesignHeight="800"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    SupportedOrientations="Portrait"  Orientation="Portrait"
    shell:SystemTray.IsVisible="False">

    <!--LayoutRoot contains the root grid where all other page content is placed-->
    <Grid x:Name="LayoutRoot">
        <controls:Panorama Title="Dude">
            <controls:Panorama.Background>
                <ImageBrush ImageSource="http://d.sparklr.me/i/b591.jpg" AlignmentX="Left" AlignmentY="Top" Stretch="UniformToFill"/>
            </controls:Panorama.Background>
            <controls:Panorama.TitleTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="0,64,0,0">
                        <Border Width="100" Height="100" CornerRadius="50" Margin="0,0,20,0">
                            <Border.Background>
                                <ImageBrush Stretch="UniformToFill" ImageSource="http://d.sparklr.me/i/591.jpg" />
                             </Border.Background>
                        </Border>
                        <TextBlock Text="{Binding}" FontSize="100" />
                    </StackPanel>
                </DataTemplate>
            </controls:Panorama.TitleTemplate>
            <!--Panorama item one-->
            <controls:PanoramaItem Header="About">
                <Grid/>
            </controls:PanoramaItem>

            <!--Panorama item two-->
            <controls:PanoramaItem Header="Posts">
                <Grid/>
            </controls:PanoramaItem>
        </controls:Panorama>
    </Grid>

</phone:PhoneApplicationPage>